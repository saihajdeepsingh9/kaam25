# URL of the Fastify API. In local dev this points at apps/server running on :4000.
# In production this is the Render service URL (e.g. https://kaam25-api.onrender.com).
NEXT_PUBLIC_API_URL=http://localhost:4000
